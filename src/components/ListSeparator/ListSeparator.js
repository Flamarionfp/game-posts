import React from 'react';
import { View } from 'react-native';

export default ListSeparator = () => {
  return (
    <View
      style={{
        height: 30,
        width: '100%',
      }}
    />
  );
};
